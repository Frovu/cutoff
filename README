API REFERENCE:

    POST /instance
        spawn calculations instance
            body - settings
        returns
            200 id
            400 // bad settings
            500 'error message'
            503 'busy' // too many instances

    GET /instance/:uuid
        get instance data
        status
            'running'   - calculating things
            'failed'    - Cutoff process dead
            'completed' - completed
        return
            102 // instance processing
            200 {
                particles: [[energy, status, time], ...],
                lower: energy,
                upper: energy,
                effective: energy
            }
            404
            500 'error'

    GET /instance/:uuid/:trace (where :trace is energy)
        get trace data (calculate if not exist)m can take some seconds
        return
            102 // instance processing
            200 [[t, x, y, z],...]
            404
            500 'error message'
            503 'too many traces'

    POST /instance/:uuid/kill
        stop processing instance and remove it
        return
            200 uuid
            402 // if instance is not processing
            404

BEHAVIOR DESCRIPTION:
    calculates some things and draws cool colored traces
